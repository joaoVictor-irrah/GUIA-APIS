# 8️⃣ Glossário de Termos

[← Voltar ao Índice](./README.md)

> **Tempo de leitura:** ~5 minutos  
> **Nível:** Referência

---


### A

**API (Application Programming Interface)**
Interface que permite que sistemas diferentes se comuniquem.

**Autenticação**
Processo de verificar a identidade de um usuário (quem você é).

**Autorização**
Processo de verificar permissões de um usuário (o que você pode fazer).

### B

**Bandwidth (Largura de Banda)**
Quantidade de dados que pode ser transmitida em determinado tempo.

**Bearer Token**
Token de autenticação enviado no cabeçalho `Authorization: Bearer TOKEN`.

### C

**Cache**
Armazenamento temporário de dados para acesso mais rápido.

**CORS (Cross-Origin Resource Sharing)**
Mecanismo que permite que uma página web acesse recursos de outro domínio.

**CRUD**
Create (Criar), Read (Ler), Update (Atualizar), Delete (Deletar).

### D

**Deploy**
Processo de colocar uma aplicação em produção.

**Downstream**
Serviço que recebe requisições de outro serviço.

### E

**Endpoint**
URL específica de uma API (ex: `/api/usuarios/123`).

**Event-Driven**
Arquitetura onde ações são disparadas por eventos.

### F

**Failover**
Transferência automática para sistema backup quando o principal falha.

### H

**Handshake**
Processo inicial de negociação em uma conexão (TLS, WebSocket).

**HMAC**
Código de autenticação de mensagem baseado em hash (usado em assinaturas).

### I

**Idempotência**
Propriedade onde executar mesma operação múltiplas vezes tem o mesmo resultado.

**IP (Internet Protocol)**
Endereço numérico que identifica um dispositivo na rede.

### J

**JSON (JavaScript Object Notation)**
Formato leve de troca de dados `{"chave": "valor"}`.

**JWT (JSON Web Token)**
Token de autenticação em formato JSON codificado.

### L

**Latência**
Tempo que uma requisição leva para ir e voltar.

**Load Balancer**
Distribui requisições entre múltiplos servidores.

### M

**Middleware**
Software intermediário que processa requisições (ex: autenticação).

**Monolito**
Aplicação onde tudo está em um único código/deploy.

### O

**OAuth**
Protocolo de autorização para acesso delegado (ex: "Login com Google").

### P

**Payload**
Dados efetivos sendo transmitidos (corpo da requisição/resposta).

**Polling**
Técnica onde cliente pergunta repetidamente por atualizações.

**Proxy**
Servidor intermediário entre cliente e servidor final.

### R

**Rate Limiting**
Limitar número de requisições que um cliente pode fazer em um período.

**REST (Representational State Transfer)**
Estilo arquitetural para APIs web.

**Retry**
Tentar novamente após falha.

### S

**SSL/TLS**
Protocolo de segurança que criptografa comunicação.

**Stateless**
Servidor não guarda estado de requisições anteriores.

### T

**Throughput**
Quantidade de dados/requisições processadas por unidade de tempo.

**Timeout**
Tempo máximo de espera por uma resposta antes de considerar falha.

**TTL (Time To Live)**
Tempo que um dado pode ser guardado em cache.

### U

**Upstream**
Serviço que envia requisições para outro serviço.

**URI/URL**
Endereço de um recurso na web.

### W

**Webhook**
Notificação HTTP enviada automaticamente quando um evento ocorre.

**WebSocket**
Protocolo de comunicação bidirecional em tempo real.

---

---

## 📚 Próximos Passos

Continue aprendendo:

➡️ **[Cenários Práticos de Suporte](./09-Cenarios-Praticos.md)**

---

[← Voltar ao Índice](./README.md)
